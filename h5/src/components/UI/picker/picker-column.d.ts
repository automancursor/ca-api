import * as React from 'react';
import { PickerColumnProps } from './interface';
import './style';
declare const PickerColumn: React.FC<PickerColumnProps> & {
    defaultProps: Partial<PickerColumnProps>;
};
export default PickerColumn;
