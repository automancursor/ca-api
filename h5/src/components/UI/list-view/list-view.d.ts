import * as React from 'react';
import { ListViewProps } from './interface';
import './style';
declare const ListView: React.SFC<ListViewProps> & {
    defaultProps: Partial<ListViewProps>;
};
export default ListView;
