import * as React from 'react';
import { DivideProps } from './interface';
import './style/index.scss';
declare const Divide: React.SFC<DivideProps> & {
    defaultProps: Partial<DivideProps>;
};
export default Divide;
