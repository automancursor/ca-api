import * as React from 'react';
import { TagCheckedProps } from './interface';
declare const TagChecked: React.FC<TagCheckedProps> & {
    defaultProps: Partial<TagCheckedProps>;
};
export default TagChecked;
